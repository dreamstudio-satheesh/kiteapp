# streamer/Dockerfile
FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt ./
COPY dummy_streamer.py .

RUN pip install --no-cache-dir -r requirements.txt

CMD ["python", "dummy_streamer.py"]
# Expose the port if needed
